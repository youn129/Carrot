// const router = require('express').Router();

// router.get('/notifications', (req, res) => {
//     const userId = req.user ? req.user.id : null; // 유저 정보에서 userId 가져오기
//     res.render('chatNotification.ejs', { userId });
// });

// module.exports = router;
