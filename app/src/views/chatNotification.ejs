<!-- <!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat Notifications</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="/main.css" rel="stylesheet">
</head>
<body class="grey-bg">
   
    <% include('nav.ejs', { userId: userId }) %>

    
    <div class="container mt-4">
        <h2>Chat Notifications</h2>
        <div id="notification-list"></div>
    </div>

    <script>
        let eventSource = new EventSource('/stream/list');
        eventSource.addEventListener('chatChange', function(e) {
            let chatNotification = JSON.parse(e.data);
            showChatNotification(chatNotification);
        });

        function showChatNotification(chatNotification) {
            let notificationList = document.getElementById('notification-list');
            let notificationItem = document.createElement('div');
            notificationItem.classList.add('alert', 'alert-info', 'mt-2');
            notificationItem.innerHTML = `
                <strong>${chatNotification.senderId}</strong> says: ${chatNotification.chatContent} at ${new Date(chatNotification.date).toLocaleString()}
            `;
            notificationList.appendChild(notificationItem);
            setTimeout(() => {
                notificationItem.remove();
            }, 5000);
        }
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html> -->
