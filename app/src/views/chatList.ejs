<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document : list</title>
</head>
<body class="grey-bg">
    <%- include('nav.ejs') %>  

    <div class="white-bg">
        <% for (var i = 0; i < result.length; i++){ %>
            <div class="list-box">
                <h4>
                    <a href="/chat/detail/<%= result[i]._id %>?senderId=<%= userId %>&partnerId=<%= (result[i].member[0] === userId ? result[i].member[1] : result[i].member[0]) %>">
                        채팅방 <%= i %>
                    </a>
                    <% if (result[i].unreadCount > 0) { %>
                        <span class="badge badge-danger"><%= result[i].unreadCount %></span>
                    <% } %>
                </h4>
                <p>
                    <a href="/chat/detail/<%= result[i]._id %>?senderId=<%= userId %>&partnerId=<%= (result[i].member[0] === userId ? result[i].member[1] : result[i].member[0]) %>">
                        <%= (result[i].member[0] === userId ? result[i].memberNames[1] : result[i].memberNames[0]) %>
                    </a>
                </p> 
                <p>
                    최근 메시지: <%= result[i].recentMessage %>
                </p>   
                <form action="/chat/delete/<%= result[i]._id %>" method="post" style="display:inline;">
                    <input type="hidden" name="userId" value="<%= userId %>">
                    <button type="submit" class="btn btn-danger">삭제</button>
                </form>
            </div>
        <% } %> 
    </div> 
</body>
</html>
